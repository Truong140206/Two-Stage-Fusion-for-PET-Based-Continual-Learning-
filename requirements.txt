# Core. torch and torchvision were missing from this file: anyone who cloned the
# repository and followed the README failed at the first import.
torch
torchvision
timm==0.6.7

numpy
scipy
scikit-learn
Pillow
requests

# six.moves.urllib is used by the dataset download helper.
six

# Only needed by tools/imagenet_a_from_parquet.py, which rebuilds ImageNet-A
# and ImageNet-R from their parquet mirrors.
pyarrow

# Only needed to run the tests.
pytest
