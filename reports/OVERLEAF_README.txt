MAIN FILE: lncs_method_en.tex
COMPILER: XeLaTeX (not pdfLaTeX)

Upload the complete ZIP as a new Overleaf project, or replace the matching
files in your existing project. Set lncs_method_en.tex as Main document.
Select XeLaTeX, then Recompile from scratch.

The ZIP includes the source, the current Springer llncs.cls, mallard.jpg,
and FIGURE_SOURCES.txt. The image is at the ZIP root; the source also supports
figure_assets/mallard.jpg for the repository layout.

The pipeline and both charts are editable vector TikZ/PGFPlots in the main
source. No shell-escape, external plotting program, custom font upload, or
BibTeX run is required. References are embedded in the source.

Fonts: Latin Modern Roman/Sans/Mono, supplied by TeX Live.
Keep default Springer margins and font sizes. SOICT2026 allows12 pages
excluding references; do not count the reference-only pages against that limit.

The main result table separates published context, our four-seed comparisons,
the overlapping seed42 grid, and a separate one-order AugReg/RanPAC comparison.
The last group is exploratory. Pending class-order runs are not included.
